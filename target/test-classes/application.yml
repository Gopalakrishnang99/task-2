spring:
  datasource:
    url: jdbc:h2:mem:test;MODE=PostgreSQL;TIME ZONE=UTC
    username:
    password:
    driverClassName: org.h2.Driver
  sql:
    init:
      mode: never
  batch:
    jdbc:
      initialize-schema: always